cxx_project(
    name = 'whiskeyToF',
    srcs = [],
    platform_srcs = [
        ('mateos', glob(['**/*_mateos.cpp'],
                        excludes = ['*test.*'])),
        ('mac', glob(['**/*_mac.cpp']))
    ],
    headers = cxx_header_glob(['.']),
)