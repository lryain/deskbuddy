
cxx_project(
    name = 'util',
    srcs = cxx_src_glob(['.'], excludes = ['ptree/*']),
    platform_srcs = [
        ('mateos', glob(['**/*_mateos.cpp'])),
        ('mac', glob(['**/*_iososx.mm']))
    ],
    headers = cxx_header_glob(['.'], excludes = ['ptree/*']),
    platform_headers = [
        ('mateos', glob(['**/*_mateos.h'])),
        ('mac', glob(['**/*_iososx.h']))
    ]
)
    
